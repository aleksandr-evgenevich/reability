{
  "name": "myproject",
  "version": "1.0.0",
  "description": "Add your OpenTok credentials to the `options` hash in  `app.js`: ```javascript const options = { credentials: {   apiKey: \"YOUR_API_KEY\",   sessionId: \"YOUR_SESSION_ID\",   token: \"YOUR_TOKEN\" }, ... ```",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "pitch-detect": "0.0.2"
  }
}
